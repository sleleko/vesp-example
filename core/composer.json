{
    "name": "vesp/example",
    "type": "template",
    "license": "MIT",
    "description": "Vesp example",
    "authors": [
        {
            "name": "Vasily Naumkin",
            "email": "bezumkin@yandex.ru"
        }
    ],
    "autoload": {
        "psr-4": {
            "App\\": "src/"
        }
    },
    "require": {
        "php": ">=7.3",
        "ext-json": "*",
        "vesp/core": "^2.2.1"
    },
    "suggest": {
        "itsgoingd/clockwork": "Debug requests with built-in middleware"
    },
    "scripts": {
        "cs-check": "vendor/bin/phpcs --colors -p",
        "cs-fix": "vendor/bin/phpcbf --colors -p"
    },
    "config": {
        "sort-packages": true
    }
}
